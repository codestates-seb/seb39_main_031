export type participate_product = {
  ended_time: string;
  goal_num: number;
  state_num: number;
  title: string;
  image_uri: string;
  base_price: number;
  unit: string;
};

export type participate_Info = {
  base_price: number;
  goal_num: number;
};
