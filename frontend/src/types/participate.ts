export type participate_product = {
  ended_time: string;
  goal_num: string;
  state_num: string;
  title: string;
  image_uri: string;
};

export type participate_Info = {
  base_price: string;
  goal_num: string;
};
